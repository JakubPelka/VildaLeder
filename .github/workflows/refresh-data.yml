name: Data refresh ownership notice

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  local-server-owned:
    runs-on: ubuntu-latest
    steps:
      - name: Explain refresh ownership
        run: |
          echo "The complete refresh runs on the always-on VildaLeder server at 03:00 Europe/Stockholm."
          echo "See scripts/server_refresh.sh and deploy/systemd/vildaleder-refresh.timer."
